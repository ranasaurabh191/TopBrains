@model StudentManagmentSystem.Models.Course

<div class="card">
    <div class="card-header d-flex align-items-center justify-content-between">
        <h3 class="mb-0 text-white">Delete Course</h3>
    </div>
    <div class="card-body">
        <p class="mb-3">Are you sure you want to delete this course?</p>
        <p><strong>@Model.CourseName</strong></p>

        <form asp-action="Delete" method="post" class="mt-3">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="CourseId" />
            <button type="submit" class="btn btn-danger btn-modern">Delete</button>
            <a asp-action="Index" class="btn btn-secondary btn-modern ms-2">Cancel</a>
        </form>
    </div>
</div>
